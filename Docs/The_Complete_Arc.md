# The Complete Arc

### One capacity field, from the logical gap to an expanding universe

*The single map of the whole program. `The_Measured_Bridge.md` reports Act I
(the generative gap and its instanton number `κ`); `The_Emergent_Cosmos.md`
reports Act II (the same `κ` as gravity, matter, and cosmos). This document sits
above both: it states the one thesis, walks the whole arc at altitude, reports
the honest bridge **between** the acts, and collects the boundaries — so the
entire structure can be seen, and built on, at once.*

---

## 1. The one thesis

The Universal Recursion Principle proposes a single functional behind structure:

    S  =  ΔC  +  κ · ΔI

— a system grows by making **distinctions** (`ΔC`, new representational content)
and **integrating** them (`ΔI`, binding content into coherent structure), traded
at an exchange rate `κ`: *the fraction of what is distinguished that can actually
be integrated.* Because a recursive system can always distinguish more than it
can integrate — the **generative gap** — the process never closes; and in failing
to close, it builds. `κ` is the one constant of that trade.

The whole program is the claim that this single `κ` shows up twice: as a
**number in the QCD vacuum** (Act I) and as **gravity, matter, and the cosmos**
(Act II) — because the capacity field that sets the exchange rate is the same
field whose free energy binds structure at every scale.

---

## 2. Act I — the measured bridge: the gap carries a number

*Full report: `The_Measured_Bridge.md`. Three measured links.*

- **The gap is real, and sharp.** The Capacity Separation Theorem
  (`I(F) < C(F) = ω₁^CK`) has a field analogue: the integrated fraction `φ = I/C`
  is **1 at N⋆ = 3** and **collapses to 0 for N ≥ 4** — a structural cliff at the
  three-fold threshold, and *capacity-invariant* (sweeping `κ` never moves it).
  Past the threshold, integration cannot be bought, only reached by changing
  structure — the theory's expressivity threshold, measured.
- **The correspondence is a functor, not an analogy.** The map from
  how-much-a-field-integrates to how-much-topology-it-carries is
  **path-independent** — cooling ladders at different rates collapse to 1.9%
  scatter at matched integration — so it preserves structure. One action-descent
  gradient `∇S` drives both ladders at once.
- **The number appears in the 4-D SU(3) vacuum.** With a genuine clover
  topological charge and the Wilson gradient flow, the **self-dual fraction**
  `f_SD = Σ|q|/Σe` — the share of the field energy carried by instanton-like
  structure — read at the RG-clean flow scale `t₀`, lands on **`κ ≈ 0.22`** at
  `β_g = 1.8`.

**Act I's honest edge:** that 0.22 is a *coarse-lattice* reading. The continuum
trend is volume-converged but cutoff-dependent, extrapolating *above* 0.22 (to
`~0.44` as `a → 0`). So what is established is a genuine, validated, RG-scaled
instanton fraction of **`O(0.2–0.4)`** — not a scheme-free pin of `κ`. The
*structure* of the bridge holds; the exact number is not yet pinned.

---

## 3. Act II — the emergent cosmos: the same κ is gravity

*Full report: `The_Emergent_Cosmos.md`. Twelve links, each an experiment with a
figure, a verdict, and a bright boundary.*

The pivot: the capacity field obeys a gradient flow
`∂_t κ = D∇²κ + r(κ₀−κ) − c·load·κ`, which descends a **free energy**
`F[κ] = ∫[(D/2)|∇κ|² + (r/2)(κ−κ₀)² + (c/2)·load·κ²]`. A field with a free energy
that responds to mass-like load *is* a theory of gravity. The arc follows that
consequence all the way to cosmology:

**Gravity and matter (Links 1–4).**
- **κ is gravity.** Load (distinction density) digs a screened `√(D/r)`-range
  well in `κ`; two masses attract with `V ∝ m₁m₂` — a Yukawa/Newton-analogue
  with an emergent equivalence principle.
- **Matter is stable form.** A particle is a **topological soliton** of the
  ψ∈ℂ³ sector field — a winding of integer charge `Q` on a Bogomolny energy
  ladder `E ∝ |Q|`, with inertial mass = gravitational mass because both are the
  same distinction density.
- **Forms gravitate, orbit, and assemble.** Self-gravitating forms infall and
  accrete; inertial dynamics give Kepler-like orbits, conserved energy,
  precession, and virialisation.

**Cosmology (Links 5–7).**
- **Structure vs expansion.** κ-gravity against a Hubble flow reproduces
  turnaround, spherical collapse, and the suppression of structure by
  expansion.
- **An expanding universe.** A genuine FLRW scale factor `a(t)`, Hubble drag
  (peculiar velocities redshift as `1/a`), and dark-energy freeze-out of growth.
- **A self-contained cosmos.** The recovery term `r(κ₀−κ)` is capacity
  *self-maintenance* — an energy that does not dilute as space expands, exactly a
  cosmological constant. So `ρ_Λ = coeff·r·κ₀²` is **derived**, and the
  decel→accel history follows.

**The Friedmann level, closed from the field (Links 8–12).**
- **Matter from forms.** `ρ_m0 ∝ Σ|Q|` from the Bogomolny spectrum, and the
  `a^{−dim}` dilution from **topological charge conservation** — the matter
  source read off the field, not dialled.
- **The equation of state.** Cold forms are pressureless **dust** (`w = 0`), the
  capacity vacuum is a **cosmological constant** (`w = −1`), measured three
  independent ways.
- **The relativistic closure.** A perfect-fluid `T^μ_ν = diag(−ρ, p, p, p)`
  whose covariant conservation `∇_μT^{μν}=0` *derives* the dilution laws and
  makes the expansion an **output**, not an input.
- **Friedmann from an action.** `H² = ρ` and `ä/a = −½(ρ+3p)` are the Hamiltonian
  constraint and Euler–Lagrange equation of a minisuperspace action — and `H²=ρ`
  is a **preserved, attracting first integral**, not a postulate.
- **Gravity from the capacity field.** That action's gravitational term `−a ȧ²`
  **is** the capacity scalar's own kinetic free energy: with `a = e^{κ_s}` (the
  scale factor as the exponential of the homogeneous capacity zero-mode),
  `−a ȧ² = −a³ κ̇_s²`. The cosmic expansion is the mean capacity **rolling**, and
  Friedmann is its energy balance `κ̇_s² = ρ`.

**Act II's honest edge:** a Newtonian-to-minisuperspace closure. The force is
screened (not `1/r²`); the dynamics is adiabatic and low-dimensional; the
identification `a = e^{κ_s}` and the wrong-sign conformal mode are readings, not
derivations; there is no inhomogeneous field theory or solved metric. What it
establishes is the **mechanism** — one field's free energy as gravity, its stable
forms as matter, its self-maintenance as dark energy, its zero-mode as the cosmic
scale — a whole cosmos out of one field, at the Friedmann level.

---

## 4. The bridge between the acts — honestly mapped

The thesis says Act I's `κ` (integration exchange rate in the gauge vacuum) and
Act II's `κ` (the capacity field of gravity) are **the same field**. Making that
a *measured number* is the natural capstone — and here the program did something
it is built to do: it **tested the identity and reported that it does not yet
hold in the obvious way**, with a mechanism.

- **One operator, two sectors.** The Bogomolny coherent fraction
  `κ̂ = Σ|q|/Σe ∈ [0,1]` is genuinely one function — it *is* both the SU(3)
  self-dual fraction and the CP² coherent fraction (verified equal). The concept
  transfers cleanly.
- **But not one number.** Under the flow the SU(3) `κ̂` **rises** to 0.22; the
  CP² `κ̂` **falls**. The sharper dimensionless invariant `⟨Q²⟩/⟨S⟩` is `~10⁻⁴`
  (gauge) vs `~10⁻²` (CP). Two natural estimators, two non-matches.
- **The mechanism.** 4-D SU(3) instantons are (near) scale-invariant — they
  **survive** the flow and emerge from UV noise. 2-D CP² instantons are
  scale-relevant — they **shrink and annihilate**. Same flow, opposite fate; the
  two sectors' topology lives at different, *moving* scales, so no `q(x),e(x)`
  estimator can coincide.

So the one-κ identity, if real, is **not** a raw lattice coincidence between two
theories whose instantons renormalise oppositely. A genuine bridge would need to
match the *physical instanton scales* first (a renormalisation condition) or be a
framework-level *definition*. This is not a failure to be papered over — it is
the frontier turned **precise**, which is what lets it eventually be crossed.

- **The renormalisation condition, imposed — and the gap survives it.** The
  scale-matched bridge (`n3_scale_matched_bridge.py`, with the
  `instanton_scales` instrument: peak-height instanton sizes from the exact
  BPST/CP profiles, and a genuine CP gradient flow whose diffusion
  normalisation is *measured*, `D = 1.001`) compares the two sectors at equal
  smoothing-per-instanton-size, `s = λ(t)/ρ̄(t)`. The condition is
  implementable (a real matched window, `s ∈ [1.27, 1.54]`) and
  coupling-robust (the CP curve at `β = 1.7` vs `2.2` agrees to `≤ 0.007`),
  and the mismatch **still stands**: minimum gap `|κ̂_I − κ̂_II| = 0.211`, the
  4-D vacuum ~3× more coherent than the 2-D one at matched relative scale.
  The obstruction's first escape route is now closed by measurement: the
  disagreement is intrinsic to the 4-D-vs-2-D comparison, not a moving-scale
  artifact.
- **The like-for-like build — and the first meeting point.** The remaining
  measurable route was a **4-D sector field** (`sector_field_4d.py`): the
  composite U(1) field strength of ψ∈ℂ³ carries a genuine second-Chern charge
  (`c₁∪c₁`, exactly integer on factorised fluxes) with the same Bogomolny
  structure, so both κ̂'s are finally read with one operator, one dimension,
  one flow clock (`n3_4d_sector_bridge.py`). On that shared clock the gauge
  κ̂ **rises** and the sector κ̂ still **falls** — dimension does not equalise
  the RG character of the two topologies — so the curves **cross**, and the
  crossing's *location* is the measurement: `κ⋆ = 0.231` on the primary
  clock, essentially at `t₀`, with a coupling/coordinate window of
  `[0.18, 0.29]` (mean 0.234, spread 0.111). The two acts' coherent fractions
  meet at a matched point in equal dimension, in the neighbourhood of Act I's
  0.22 — **a window, not yet a value**: the stability of that location is now
  the sharp open question.

---

## 5. The honest boundaries, collected

The arc is real and its edges are bright. Across the whole program, what it is
**not**:

- **Act I does not pin `κ`.** The self-dual fraction is a validated instanton
  observable of `O(0.2–0.4)`, volume-converged but cutoff-dependent; 0.22 is a
  coarse-lattice reading, not a scheme-free determination. It does not prove the
  metaphysical identity of logical incompleteness with the QCD vacuum.
- **Act II is not general relativity.** `κ`-gravity is a screened, Newton's-`G`
  analogue, not the spacetime metric; the dynamics is adiabatic and
  low-dimensional; the Friedmann-level closure is minisuperspace (homogeneous,
  one degree of freedom), with the gravitational action's form and the
  `a = e^{κ_s}` identification posited, not derived from a covariant `κ`-action.
- **The two acts are not yet numerically one.** The shared `κ̂` operator is one
  function, but the two sectors do not give a matched number in any lattice
  topological estimator tested — including the scale-matched one, where the
  obstruction's own renormalisation condition is imposed and a coupling-robust
  gap of `≈ 0.21` survives. The reason is understood (RG fate of instantons;
  an intrinsically more coherent 4-D vacuum), not an unexamined gap.

These boundaries are load-bearing: they are what make the parts that *do* hold
credible.

---

## 6. Where it stands, and the open frontiers

**Established.** A single capacity field with one free energy reproduces — as
*measured mechanisms*, each with tests and an honest scope — the generative gap
and its instanton fraction (Act I); and gravity, the equivalence principle,
orbits, cosmic structure, an expanding universe with derived dark energy, matter
from topological forms, its equation of state, a conserved stress-energy tensor,
the Friedmann equations as a variational constraint, and the gravitational action
as the capacity field's own kinetic energy (Act II). The two-act bridge is a
precisely-characterised frontier.

**Open, in rough order of depth.**
1. **Inhomogeneity.** Lift the homogeneous capacity zero-mode `κ_s = ln a` to a
   field `δκ_s(x,t)` — cosmological perturbations, a real growth factor `D(a)`, a
   solved metric — reconnecting the N-body structure work on a relativistic
   footing.
2. **The one-κ crossing, stabilised.** Both routes named by the obstruction
   have now been walked: the scale-matching condition is imposed and the
   4-D-vs-2-D gap survives it (`n3_scale_matched_bridge.py`); the like-for-like
   4-D sector field is built and its κ̂ **crosses** the gauge κ̂ near `t₀` and
   near 0.22 — but in a window of ±0.06 across couplings and matching
   coordinates (`n3_4d_sector_bridge.py`). The frontier is now concrete:
   larger lattices, a denser coupling scan, and an explanation of *why* the
   primary crossing tracks `t₀` — or the framework-level definition, of which
   the near-0.22 crossing may be one estimator's shadow.
3. **Pinning Act I's `κ`.** Genuinely finer SU(3) lattices (against topological
   freezing) and the matched operator-product-expansion condensate the self-dual
   fraction stands in for.
4. **A covariant κ-action.** Derive the gravitational term and the metric
   coupling from `κ` itself, beyond minisuperspace — the step from "gravity's
   cosmology from the field" to "gravity from the field."

---

## 7. The map — where everything lives

**Documents.**
| file | what it holds |
|---|---|
| `The_Generative_Gap.md` | the `S = ΔC + κ·ΔI` program and thread |
| `The_Measured_Bridge.md` | **Act I** capstone — the gap → `κ` in the SU(3) vacuum |
| `The_Emergent_Cosmos.md` | **Act II** capstone — `κ` as gravity, matter, cosmos (12 links + the frontier) |
| `Capacity_As_Gravity.md` | the running κ-gravity derivation |
| `The_Complete_Arc.md` | *this document* — the whole program, both acts, the bridge |

**Modules** (`project_genesis/`): `multiphase.py` (the κ dynamics),
`topological_charge.py` (CP² charge, cooling, the shared `coherent_fraction`
operator + thermal sampler), `gauge_topology.py` (4-D SU(3) charge, Wilson flow,
self-dual fraction), `instanton_scales.py` (peak-height instanton sizes, the CP
gradient flow with measured diffusion, the matched-scale comparison),
`sector_field_4d.py` (the 4-D sector field: composite U(1) field strength,
second-Chern charge, dimension-generic sampler and flow),
`gauge_mc.py` (the gauge ensemble), `capacity_gravity.py`
(screened wells), `stable_forms.py` (topological solitons as matter),
`capacity_dynamics.py` (the whole Act II dynamics: gravity, orbits, cosmology,
stress-energy, the variational and capacity-scalar closures).

**Experiments** (`experiments/n3_*.py`): one per link, each writing a figure and
a verdict to `artifacts/` — from `n3_capacity_separation.py` (the gap) through
`n3_su3_gradient_flow.py` (the number) to `n3_gravity_from_capacity.py` (gravity
from the field), `n3_kappa_obstruction.py` (the frontier),
`n3_scale_matched_bridge.py` (the frontier's condition, imposed), and
`n3_4d_sector_bridge.py` (the like-for-like crossing).

**Tests** (`tests/`): every module helper and every link's central claim, so the
structure is a standing, checkable record — the memory that lets the arc be built
on rather than re-argued.

---

*One field, distinguishing more than it can integrate, and building a world in
the shortfall. From a cliff in logic to a number in the vacuum to a rolling
scale factor — measured link by link, with every boundary kept bright. The arc,
as far as honest measurement takes it, is mapped.*
